---
phase: 01-platform-professionalization
plan: 06
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified:
  - src/hooks/use-patient-data.ts
  - src/pages/admin/Overview.tsx
  - src/pages/admin/PracticeDetail.tsx
  - src/pages/admin/Practices.tsx
  - src/pages/admin/Settings.tsx
  - src/pages/admin/Patients.tsx
  - src/pages/admin/Support.tsx
  - src/pages/admin/System.tsx
  - src/pages/doctor/Overview.tsx
  - src/pages/doctor/PatientDetail.tsx
  - src/pages/doctor/RecordResponse.tsx
  - src/pages/doctor/ScanCompare.tsx
  - src/pages/doctor/ScanReview.tsx
  - src/pages/doctor/Settings.tsx
  - src/pages/patient/DoctorProfile.tsx
  - src/pages/public/SharedProgress.tsx
autonomous: true
requirements: [PROF-03]

must_haves:
  truths:
    - "No page crashes when one of its parallel Supabase queries fails — the page renders with partial data from successful queries"
    - "Rejected parallel queries are logged with logError, not silently swallowed"
    - "All 17 Promise.all() sites have been replaced with Promise.allSettled()"
    - "Each rejected result is extracted with status check and logged before falling back to null"
  artifacts:
    - path: "src/pages/admin/Overview.tsx"
      provides: "7-way Promise.allSettled — highest-value conversion"
      contains: "Promise.allSettled"
    - path: "src/pages/doctor/Settings.tsx"
      provides: "5-way Promise.allSettled — highest crash-risk conversion"
      contains: "Promise.allSettled"
    - path: "src/hooks/use-patient-data.ts"
      provides: "4-way Promise.allSettled"
      contains: "Promise.allSettled"
  key_links:
    - from: "all 16 modified files"
      to: "src/lib/logger.ts"
      via: "logError called for each rejected result in allSettled"
      pattern: "status.*rejected.*logError"
---

<objective>
Replace all 17 Promise.all() calls with Promise.allSettled() and add per-rejection logError calls (PROF-03). Each file continues to render with whatever data succeeded — one failed query no longer brings down the entire page.

Purpose: Promise.all() throws the moment any promise rejects, causing the catch block to fire and the entire page to show an error state. Promise.allSettled() returns results for all promises regardless of individual failures.
Output: 17 conversion sites; all pages resilient to partial fetch failures.
</objective>

<execution_context>
@C:/Users/King/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/King/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-professionalization/01-RESEARCH.md
@.planning/phases/01-platform-professionalization/01-03-SUMMARY.md

<interfaces>
<!-- Conversion pattern from research (Pattern 3) -->
// BEFORE:
const [profileRes, subRes] = await Promise.all([queryA, queryB]);

// AFTER:
const [profileResult, subResult] = await Promise.allSettled([queryA, queryB]);
const profile = profileResult.status === "fulfilled" ? profileResult.value.data : null;
const subscription = subResult.status === "fulfilled" ? subResult.value.data : null;

// Log each rejection — do not silently discard:
if (profileResult.status === "rejected") {
  logError(profileResult.reason, { operation: "ComponentName/fetchProfile", userId: user?.id });
}

<!-- Optional settled() helper to reduce boilerplate (can be defined locally in each file or in a shared util) -->
function settled<T>(result: PromiseSettledResult<{ data: T | null }>): T | null {
  return result.status === "fulfilled" ? result.value.data : null;
}

<!-- File + line inventory from research (all 17 sites) -->
// use-patient-data.ts line 40 (4-way) — NOTE: may already be converted in Plan 05 Task 1; check first
// admin/Overview.tsx line 17 (7-way) — highest value
// admin/PracticeDetail.tsx line 28 (5-way)
// admin/Practices.tsx line 37 (2-way)
// admin/Settings.tsx line 30 (3-way)
// admin/Patients.tsx line 30 (2-way)
// admin/Support.tsx line 27 (3-way)
// admin/System.tsx line 30 (mutation chain — also convert to allSettled, check each result)
// doctor/Overview.tsx line 71 (3-way)
// doctor/PatientDetail.tsx lines 28 and 142 (2-way each)
// doctor/RecordResponse.tsx line 39 (2-way) — also has .maybeSingle() from Plan 05
// doctor/ScanCompare.tsx lines 22 and 30 (2-way each)
// doctor/ScanReview.tsx line 60 (2-way)
// doctor/Settings.tsx line 53 (5-way) — highest crash risk
// patient/DoctorProfile.tsx line 46 (2-way)
// public/SharedProgress.tsx line 34 (3-way)
// use-auth.ts line 68 — already converted in Plan 02; DO NOT TOUCH
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert Promise.all() to Promise.allSettled() — hooks and high-value admin pages</name>
  <files>
    src/hooks/use-patient-data.ts
    src/pages/admin/Overview.tsx
    src/pages/admin/PracticeDetail.tsx
    src/pages/admin/Settings.tsx
    src/pages/admin/System.tsx
  </files>
  <behavior>
    - use-patient-data.ts: 4-way allSettled; each failed result logged; partial data returned
    - admin/Overview.tsx: 7-way allSettled; page renders with whichever datasets loaded successfully
    - admin/PracticeDetail.tsx: 5-way allSettled; detail page shows partial data on failure
    - admin/Settings.tsx: 3-way allSettled
    - admin/System.tsx: mutation chain allSettled; each failed mutation is explicitly checked and logged (not silently ignored)
    - Test: promise-resilience.test.ts passes GREEN after these changes
  </behavior>
  <action>
    For each file:

    1. If logError import is not already present: add `import { logError } from "@/lib/logger";`

    2. Locate the Promise.all() call(s) per the line inventory.

    3. Apply the conversion:
       - Change `await Promise.all([a, b, c])` to `await Promise.allSettled([a, b, c])`
       - Rename result variables from `[aRes, bRes]` to `[aResult, bResult]` to avoid shadowing
       - Extract data: `const aData = aResult.status === "fulfilled" ? aResult.value.data : null;`
       - Add rejection logging for each: `if (aResult.status === "rejected") logError(aResult.reason, { operation: "ComponentName/functionName", userId: user?.id });`

    4. Update any downstream code that used the old variable names to use the new extracted values.

    admin/System.tsx special case: This is a mutation chain. The conversion is the same, but each rejected mutation should not silently continue — log AND show a toast for each failure:
    ```
    if (mutationResult.status === "rejected") {
      logError(mutationResult.reason, { operation: "admin/System/applyConfig" });
      toast({ title: "Some settings failed to save", variant: "destructive" });
    }
    ```

    NOTE: use-patient-data.ts may already have this converted from Plan 05 Task 1. Check the file first — if allSettled is already there, skip it.
  </action>
  <verify>
    <automated>npm test -- src/test/promise-resilience.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 files converted; promise-resilience.test.ts passes GREEN; each rejection path calls logError</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Promise.all() to Promise.allSettled() — remaining admin, doctor, and patient pages</name>
  <files>
    src/pages/admin/Practices.tsx
    src/pages/admin/Patients.tsx
    src/pages/admin/Support.tsx
    src/pages/doctor/Overview.tsx
    src/pages/doctor/PatientDetail.tsx
    src/pages/doctor/RecordResponse.tsx
    src/pages/doctor/ScanCompare.tsx
    src/pages/doctor/ScanReview.tsx
    src/pages/doctor/Settings.tsx
    src/pages/patient/DoctorProfile.tsx
    src/pages/public/SharedProgress.tsx
  </files>
  <behavior>
    - All 11 files converted from Promise.all to Promise.allSettled
    - doctor/Settings.tsx (5-way) — highest crash risk — now renders with partial data on any single failure
    - PatientDetail.tsx: both sites at lines 28 and 142 converted
    - ScanCompare.tsx: both sites at lines 22 and 30 converted
    - RecordResponse.tsx: site at line 39 converted (note: .maybeSingle() already applied to queries inside by Plan 05)
    - All rejections logged with logError
  </behavior>
  <action>
    Apply the same conversion pattern as Task 1 to the remaining 11 files.

    For each file in the list:
    1. Check if logError is imported — add import if not present.
    2. Find the Promise.all() site(s) per line inventory.
    3. Convert to Promise.allSettled() with extraction and rejection logging.
    4. Update downstream code using the old result variable names.

    doctor/Settings.tsx special attention: This is a 5-way parallel query (line 53). The five queries each load a different part of the settings page. After conversion:
    - If any result is rejected, log it and let that section show its empty/default state
    - Do not redirect or throw on partial failure

    doctor/PatientDetail.tsx: Two Promise.all sites (lines 28 and 142 per research). Convert both independently.

    doctor/ScanCompare.tsx: Two Promise.all sites (lines 22 and 30). Convert both.

    After completing all files, run a grep to confirm zero Promise.all remain (except in use-auth.ts which is already allSettled):
    grep -rn "Promise\.all(" src/pages/ src/hooks/ | grep -v "allSettled"
    Expected output: empty (no results).
  </action>
  <verify>
    <automated>npm test -- src/test/promise-resilience.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 17 Promise.all sites converted to Promise.allSettled; zero Promise.all remain in pages/ and hooks/ (excluding use-auth.ts already converted); all rejection paths log with logError; promise-resilience.test.ts GREEN; npm test suite passes</done>
</task>

</tasks>

<verification>
npm test -- src/test/promise-resilience.test.ts

Grep confirmation:
grep -rn "Promise\.all(" src/pages/ src/hooks/ | grep -v "allSettled"

Expected: empty output (no Promise.all remaining outside use-auth.ts which is already converted).

npx tsc --noEmit
Expected: no TypeScript errors from allSettled result type changes.
</verification>

<success_criteria>
- All 17 Promise.all() sites converted to Promise.allSettled()
- promise-resilience.test.ts passes GREEN
- Each rejected result calls logError with operation context
- No silent swallowing of errors
- admin/System.tsx mutation failures show toast notifications
- Full npm test suite passes
</success_criteria>

<output>
After completion, create .planning/phases/01-platform-professionalization/01-06-SUMMARY.md
</output>
