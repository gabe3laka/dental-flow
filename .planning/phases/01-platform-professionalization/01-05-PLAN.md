---
phase: 01-platform-professionalization
plan: 05
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified:
  - src/hooks/use-patient-data.ts
  - src/hooks/use-feature-flag.ts
autonomous: true
requirements: [PROF-02, PROF-06]

must_haves:
  truths:
    - "No page crashes with a white screen when a database row is missing — maybeSingle null guards in both hooks return gracefully instead of throwing"
    - "All errors surfaced from hook queries include operation context, user context, and timestamp via logError"
  artifacts:
    - path: "src/hooks/use-patient-data.ts"
      provides: "3 maybeSingle conversions + null guards + logError + Promise.allSettled at line 40"
      contains: "maybeSingle"
    - path: "src/hooks/use-feature-flag.ts"
      provides: "1 maybeSingle conversion + null guard + logError"
      contains: "maybeSingle"
  key_links:
    - from: "src/hooks/use-patient-data.ts"
      to: "src/lib/logger.ts"
      via: "import { logError } from '@/lib/logger'"
      pattern: "logError"
    - from: "src/hooks/use-feature-flag.ts"
      to: "src/lib/logger.ts"
      via: "import { logError } from '@/lib/logger'"
      pattern: "logError"
---

<objective>
Convert all SELECT .single() calls to .maybeSingle() with null guards (PROF-02) and replace console.error() calls with structured logError() calls (PROF-06) in the two shared hooks that are consumed across many pages.

Purpose: Must complete before 01-05b so the page sweep can assume the hook contracts are already safe. Creating the hook fixes first prevents 01-05b from needing to touch these files.
Output: use-patient-data.ts and use-feature-flag.ts with zero SELECT .single() calls, explicit null guards, and structured error logging.
</objective>

<execution_context>
@C:/Users/King/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/King/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-professionalization/01-RESEARCH.md
@.planning/phases/01-platform-professionalization/01-02-SUMMARY.md
@.planning/phases/01-platform-professionalization/01-03-SUMMARY.md

<interfaces>
<!-- CRITICAL DISTINCTION — do not confuse these two cases -->

// CONVERT: SELECT that may return 0 rows
const { data } = await supabase.from("patients").select("id").eq("user_id", uid).maybeSingle();
if (!data) { logError("Patient not found", { operation: "use-patient-data/loadPatient", userId: uid }); return; }

// DO NOT CONVERT: INSERT returning the new row (cannot be null if no error)
const { data } = await supabase.from("scans").insert({...}).select("id").single();

<!-- Null guard pattern — always include logError before the UI fallback -->
if (!data) {
  logError("Expected row missing", { operation: "use-patient-data/queryName", userId: user?.id });
  return appropriate_fallback;
}

<!-- logError import line -->
import { logError } from "@/lib/logger";

<!-- NOTE: use-auth.ts was already converted in Plan 02. Do NOT re-convert lines 73 and 78. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert SELECT .single() to .maybeSingle() in hooks</name>
  <files>
    src/hooks/use-patient-data.ts
    src/hooks/use-feature-flag.ts
  </files>
  <behavior>
    - use-patient-data.ts: 3 sites (lines 35, 64, 89) converted to .maybeSingle() each with null guard
    - use-patient-data.ts: 1 console.error call replaced with logError
    - use-patient-data.ts: Promise.all at line 40 converted to Promise.allSettled to avoid touching this file again in 01-06
    - use-feature-flag.ts: 1 site (line 11) converted to .maybeSingle() with null guard
    - Test: supabase-query-safety.test.ts passes GREEN after these changes
  </behavior>
  <action>
    use-patient-data.ts:
    1. Add import: `import { logError } from "@/lib/logger";`
    2. At each of the 3 .single() sites:
       - Change to .maybeSingle()
       - After the query, add: if (!data) { logError("Missing row", { operation: "use-patient-data/{query name}", userId: ... }); return appropriate fallback; }
    3. Replace the existing console.error call with logError, preserving the context message.
    4. Also check line 40 (Promise.all site from PROF-03 audit) — convert to Promise.allSettled here to avoid touching this file again in Plan 01-06.

    use-feature-flag.ts:
    1. Add import: `import { logError } from "@/lib/logger";`
    2. Line 11: change .single() to .maybeSingle()
    3. Add null guard: if (!data) { logError("Feature flag row missing", { operation: "use-feature-flag" }); return false; }
    4. If a console.error exists in this file, replace with logError.

    NOTE: use-auth.ts was already converted in Plan 02. Do NOT re-convert it.
  </action>
  <verify>
    <automated>npm test -- src/test/supabase-query-safety.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>use-patient-data.ts and use-feature-flag.ts have zero .single() on SELECT queries; null guards added; logError used for error logging; Promise.allSettled at the line 40 site in use-patient-data.ts; supabase-query-safety tests pass</done>
</task>

</tasks>

<verification>
npm test -- src/test/supabase-query-safety.test.ts

Grep verification (hooks only):
grep -n "\.single()" src/hooks/use-patient-data.ts src/hooks/use-feature-flag.ts | grep -v "\.insert("

Expected: empty output (no SELECT .single() remaining).

npx tsc --noEmit
Expected: no TypeScript errors from null guard additions.
</verification>

<success_criteria>
- use-patient-data.ts: all SELECT .single() converted to .maybeSingle() with null guards
- use-feature-flag.ts: .single() at line 11 converted to .maybeSingle() with null guard
- Both files import and use logError from @/lib/logger
- supabase-query-safety.test.ts passes GREEN
- Full npm test suite passes
</success_criteria>

<output>
After completion, create .planning/phases/01-platform-professionalization/01-05-SUMMARY.md
</output>
