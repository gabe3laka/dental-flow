---
phase: 01-platform-professionalization
plan: 04
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified:
  - src/pages/doctor/RecordResponse.tsx
  - src/pages/doctor/Settings.tsx
  - src/pages/doctor/PatientDetail.tsx
  - src/pages/patient/ScanSubmission.tsx
  - src/pages/public/Onboarding.tsx
  - src/pages/public/PracticeSetup.tsx
  - src/pages/admin/Automations.tsx
autonomous: true
requirements: [PROF-01, PROF-08]

must_haves:
  truths:
    - "Camera LED turns off immediately when leaving RecordResponse — stream tracks, interval, and recorder all stopped on unmount"
    - "ScanSubmission camera cleanup is verified correct and untouched (already fixed)"
    - "Every async submit button in Settings, PatientDetail, Onboarding, PracticeSetup, and Automations is disabled while its async handler is in-flight"
    - "No form can be double-submitted — clicking the button a second time before the first completes has no effect"
  artifacts:
    - path: "src/pages/doctor/RecordResponse.tsx"
      provides: "Camera useEffect cleanup that stops stream tracks, clears intervalRef, stops recorderRef"
      contains: "clearInterval(intervalRef.current)"
    - path: "src/pages/doctor/Settings.tsx"
      provides: "inviting, addingSlot, savingSpecialty, uploadingLogo states wired to button disabled props"
  key_links:
    - from: "src/pages/doctor/RecordResponse.tsx"
      to: "intervalRef / recorderRef"
      via: "useEffect cleanup return function"
      pattern: "clearInterval.*intervalRef"
    - from: "src/pages/doctor/Settings.tsx"
      to: "handleInvite / handleAddSlot / saveSpecialty / handleLogoUpload"
      via: "loading state disabled prop on each Button"
      pattern: "disabled=\\{inviting\\}"
---

<objective>
Fix the camera stream leak in RecordResponse.tsx (PROF-01) and add loading lock state to all 8 async submit handlers missing it (PROF-08). These are surgical per-file changes with no cross-file dependencies.

Purpose: Camera leak causes the browser camera LED to stay on after leaving the record page — a critical trust issue. Double-submit bugs can cause duplicate database rows and confusing UX.
Output: RecordResponse has proper unmount cleanup; all identified async buttons are disabled while in-flight.
</objective>

<execution_context>
@C:/Users/King/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/King/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-professionalization/01-RESEARCH.md
@.planning/phases/01-platform-professionalization/01-02-SUMMARY.md
@.planning/phases/01-platform-professionalization/01-03-SUMMARY.md

<interfaces>
<!-- From research PROF-01 fix (Pattern 1: Camera Stream Cleanup) -->
// RecordResponse.tsx camera useEffect return — ADD these two lines:
return () => {
  cancelled = true;
  streamRef.current?.getTracks().forEach((t) => t.stop());
  if (intervalRef.current) clearInterval(intervalRef.current);  // ADD
  recorderRef.current?.stop();                                   // ADD
};

<!-- From research PROF-08 fix template (Pattern 7) -->
// Template for each missing loading lock:
const [inviting, setInviting] = useState(false);
const handleInvite = async () => {
  if (!inviteEmail.trim() || !user) return;
  setInviting(true);
  try { /* existing logic */ }
  catch (e: any) { toast({ title: "Error", description: e.message, variant: "destructive" }); }
  finally { setInviting(false); }
};
<Button onClick={handleInvite} disabled={inviting}>
  {inviting ? "Sending..." : "Send Invite"}
</Button>

<!-- PROF-08 targets from research (7 locations requiring change) -->
// Settings.tsx: handleInvite (no inviting state), handleAddSlot (no addingSlot state),
//   saveSpecialty (no savingSpecialty state), handleLogoUpload (uploadingLogo exists but not wired to disabled)
// PatientDetail.tsx: handleSaveCost (saving state exists — verify button uses it correctly)
// PracticeSetup.tsx: saving state exists — verify all submit buttons use it
// Automations.tsx: form submission handlers — audit and add lock if missing
// Signup.tsx: already correct — DO NOT TOUCH
// ScanSubmission.tsx: already correct — verify only
// RecordResponse.tsx: already correct for submit — verify only
// Login.tsx: already correct — DO NOT TOUCH
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix RecordResponse camera cleanup (PROF-01)</name>
  <files>src/pages/doctor/RecordResponse.tsx</files>
  <behavior>
    - After change: when RecordResponse unmounts while recording is active, clearInterval is called with the intervalRef value
    - After change: when RecordResponse unmounts while recording is active, recorderRef.current.stop() is called
    - After change: stream tracks are stopped on unmount (already present — verify not broken)
    - Test: src/test/camera-cleanup.test.tsx tests go GREEN
  </behavior>
  <action>
    Open src/pages/doctor/RecordResponse.tsx. Find the camera useEffect (the one that calls navigator.mediaDevices.getUserMedia and sets streamRef.current). It is in the range of lines 60–78.

    The return function of this useEffect currently has:
    ```
    return () => {
      cancelled = true;
      streamRef.current?.getTracks().forEach((t) => t.stop());
    };
    ```

    ADD two lines to this return function:
    ```
    return () => {
      cancelled = true;
      streamRef.current?.getTracks().forEach((t) => t.stop());
      if (intervalRef.current) clearInterval(intervalRef.current);
      recorderRef.current?.stop();
    };
    ```

    Do NOT change anything else in RecordResponse.tsx in this task — PROF-02 and PROF-08 changes for RecordResponse happen in separate tasks.

    VERIFY: src/pages/patient/ScanSubmission.tsx already has correct cleanup. Confirm by reading it — if cleanup is correct, make NO changes.
  </action>
  <verify>
    <automated>npm test -- src/test/camera-cleanup.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>camera-cleanup.test.tsx tests pass GREEN; RecordResponse cleanup return contains clearInterval and recorderRef.current?.stop(); ScanSubmission.tsx is unchanged</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add loading locks to all async submit handlers (PROF-08)</name>
  <files>
    src/pages/doctor/Settings.tsx
    src/pages/doctor/PatientDetail.tsx
    src/pages/public/PracticeSetup.tsx
    src/pages/admin/Automations.tsx
  </files>
  <behavior>
    - Settings.tsx: handleInvite button is disabled while inviting===true
    - Settings.tsx: handleAddSlot button is disabled while addingSlot===true
    - Settings.tsx: saveSpecialty button is disabled while savingSpecialty===true
    - Settings.tsx: handleLogoUpload button is disabled while uploadingLogo===true (state already exists — wire to disabled)
    - PatientDetail.tsx: handleSaveCost button is disabled while saving===true (verify or fix)
    - PracticeSetup.tsx: all step submission buttons are disabled while saving===true (verify or fix)
    - Automations.tsx: any async form submission buttons are disabled while their in-flight state is true
    - Test: src/test/submit-lock.test.tsx tests pass GREEN
  </behavior>
  <action>
    For each file, follow the loading lock template from research. Do not alter any business logic.

    Settings.tsx changes:
    1. Add `const [inviting, setInviting] = useState(false);` near other useState declarations
    2. Wrap handleInvite body: setInviting(true) before try, setInviting(false) in finally
    3. Add `disabled={inviting}` to the "Send Invite" button; update label to show "Sending..." when inviting
    4. Add `const [addingSlot, setAddingSlot] = useState(false);`
    5. Wrap handleAddSlot body: setAddingSlot(true)/finally setAddingSlot(false)
    6. Add `disabled={addingSlot}` to the "Add Slot" button
    7. Add `const [savingSpecialty, setSavingSpecialty] = useState(false);`
    8. Wrap saveSpecialty body: setSavingSpecialty(true)/finally setSavingSpecialty(false)
    9. Add `disabled={savingSpecialty}` to the specialty save button
    10. Find the logo upload button — uploadingLogo state already exists. Add `disabled={uploadingLogo}` if not already present.

    PatientDetail.tsx changes:
    - Find handleSaveCost — read if `saving` state is already present and wired to the save button's disabled prop. If not wired, add `disabled={saving}` to the button. If saving state is missing entirely, add it following the template.

    PracticeSetup.tsx changes:
    - Audit all submit buttons in the multi-step form. `saving` state should already exist. Verify each step's submit/continue button has `disabled={saving}`. Add where missing.

    Automations.tsx changes:
    - Find all async handlers (they have await and interact with supabase). For each one missing a loading lock: add boolean state, wrap handler in try/finally with state toggling, add disabled prop to the trigger button.

    CRITICAL: Use logError from @/lib/logger for any error handling you add or touch. Import at top of file:
    ```typescript
    import { logError } from "@/lib/logger";
    ```
    Replace any existing catch(e) { console.error(e) } blocks you encounter with logError calls while in these files.
  </action>
  <verify>
    <automated>npm test -- src/test/submit-lock.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>submit-lock.test.tsx tests pass GREEN; all 8 identified async handlers have loading state wired to their button's disabled prop; no double-submit possible on Settings handleInvite, handleAddSlot, saveSpecialty, handleLogoUpload, PatientDetail handleSaveCost, PracticeSetup form steps, Automations submissions</done>
</task>

</tasks>

<verification>
npm test -- src/test/camera-cleanup.test.tsx src/test/submit-lock.test.tsx
Expected: all tests GREEN.

npx tsc --noEmit
Expected: no TypeScript errors in modified files.
</verification>

<success_criteria>
- camera-cleanup.test.tsx: all tests GREEN
- submit-lock.test.tsx: all tests GREEN
- RecordResponse.tsx useEffect cleanup contains clearInterval(intervalRef.current) and recorderRef.current?.stop()
- Settings.tsx handleInvite, handleAddSlot, saveSpecialty, handleLogoUpload all have loading state wired to disabled prop
- PatientDetail.tsx handleSaveCost wired to disabled
- PracticeSetup.tsx all step submit buttons wired to saving state
- Automations.tsx async handlers have loading locks
- Full npm test suite passes
</success_criteria>

<output>
After completion, create .planning/phases/01-platform-professionalization/01-04-SUMMARY.md
</output>
