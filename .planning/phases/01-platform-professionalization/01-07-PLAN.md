---
phase: 01-platform-professionalization
plan: 07
type: execute
wave: 5
depends_on: [01-04, 01-05b, 01-06b]
files_modified:
  - src/pages/public/Login.tsx
  - src/pages/public/Signup.tsx
  - src/pages/public/PracticeSetup.tsx
  - src/pages/public/Onboarding.tsx
  - src/pages/doctor/Settings.tsx
  - src/pages/doctor/PatientDetail.tsx
autonomous: true
requirements: [PROF-05]

must_haves:
  truths:
    - "Login form with invalid email shows 'Enter a valid email' inline error and does not call supabase.auth.signInWithPassword"
    - "Signup form with missing fullName shows a required field error and does not call supabase.auth.signUp"
    - "PracticeSetup form with a practice name shorter than 2 characters shows a validation error and does not persist to database"
    - "No form in the listed files accepts and persists structurally invalid data"
    - "All Zod schemas are defined at module scope (not inside component render functions)"
  artifacts:
    - path: "src/pages/public/Login.tsx"
      provides: "loginSchema (Zod) + useForm with zodResolver + FormField error display"
      contains: "zodResolver"
    - path: "src/pages/public/Signup.tsx"
      provides: "signupSchema (Zod) + useForm with zodResolver"
      contains: "zodResolver"
    - path: "src/pages/public/PracticeSetup.tsx"
      provides: "practiceSetupSchema (Zod) + useForm with zodResolver"
      contains: "zodResolver"
    - path: "src/pages/public/Onboarding.tsx"
      provides: "onboardingSchema (Zod) + useForm with zodResolver"
      contains: "zodResolver"
  key_links:
    - from: "Login.tsx / Signup.tsx / PracticeSetup.tsx / Onboarding.tsx"
      to: "zod + @hookform/resolvers"
      via: "zodResolver(schema) in useForm({ resolver: zodResolver(schema) })"
      pattern: "zodResolver"
    - from: "form components"
      to: "src/components/ui/form.tsx"
      via: "FormField + FormMessage for inline error display"
      pattern: "FormMessage"
---

<objective>
Add Zod validation schemas to all 6 forms that currently have no input validation (PROF-05). Invalid data cannot be submitted — Zod schemas enforce field rules before the async handler is called.

Purpose: Forms currently rely on HTML `required` attribute alone. Zod + zodResolver provides per-field error messages inline, type-safe form values, and prevents the submit handler from ever seeing invalid data.
Output: 6 forms with Zod schemas at module scope, useForm with zodResolver, and FormMessage components for inline error display.
</objective>

<execution_context>
@C:/Users/King/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/King/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-professionalization/01-RESEARCH.md
@.planning/phases/01-platform-professionalization/01-04-SUMMARY.md

<interfaces>
<!-- shadcn/ui form.tsx exports — use these for error display (src/components/ui/form.tsx) -->
// FormProvider, FormField, FormItem, FormLabel, FormControl, FormMessage
// FormField renders a Controller internally; FormMessage reads fieldState.error.message

<!-- Zod + react-hook-form integration pattern (from research Pattern 5: Form Schemas) -->
// IMPORTANT: Define schema at module scope — not inside component body

import { z } from "zod";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from "@/components/ui/form";

// At module scope:
const loginSchema = z.object({
  email: z.string().email("Enter a valid email"),
  password: z.string().min(6, "Password must be at least 6 characters"),
});
type LoginFormValues = z.infer<typeof loginSchema>;

// In component:
const form = useForm<LoginFormValues>({
  resolver: zodResolver(loginSchema),
  defaultValues: { email: "", password: "" },
});

// Replace manual onSubmit(e: React.FormEvent) with:
const onSubmit = form.handleSubmit(async (values: LoginFormValues) => {
  // values are type-safe and validated here
  await supabase.auth.signInWithPassword({ email: values.email, password: values.password });
});

// JSX: replace <form onSubmit={...}> with <Form {...form}><form onSubmit={onSubmit}>
// Wrap inputs with <FormField> + <FormMessage> for error display

<!-- Schemas to create (from research — all at module scope) -->
// loginSchema: { email: z.string().email(), password: z.string().min(6) }
// signupSchema: { fullName: z.string().min(2), email: z.string().email(), password: z.string().min(6), role: z.enum(["doctor","patient"]), specialty: z.string().optional() }
// practiceSetupSchema: { practiceName: z.string().min(2), specialty: z.string().min(1), address: z.string().min(5), bio: z.string().min(10), yearsPractice: z.number().min(0).max(60) }
// onboardingSchema: category-specific fields (read actual field names from the component)
// Settings invite schema: { inviteEmail: z.string().email() }
// PatientDetail cost schema: { estimatedCost: z.number().min(0) }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Zod validation to Login and Signup forms</name>
  <files>
    src/pages/public/Login.tsx
    src/pages/public/Signup.tsx
  </files>
  <behavior>
    - Login: empty email → "Enter a valid email" message shown inline; supabase.signInWithPassword NOT called
    - Login: password shorter than 6 chars → "Password must be at least 6 characters" shown inline
    - Login: valid inputs → form submits normally, existing auth logic unchanged
    - Signup: missing fullName → required field error shown inline; supabase.signUp NOT called
    - Signup: invalid email format → email error shown inline
    - Signup: password < 6 chars → password error shown inline
    - Test: form-validation.test.tsx Login and Signup tests pass GREEN
  </behavior>
  <action>
    Login.tsx:
    1. Add imports at top: `import { z } from "zod"; import { useForm } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from "@/components/ui/form";`
    2. At module scope (OUTSIDE the component function): define loginSchema and LoginFormValues type.
    3. Inside the component: replace `const [email, setEmail] = useState("")` and `const [password, setPassword] = useState("")` with `const form = useForm<LoginFormValues>({ resolver: zodResolver(loginSchema), defaultValues: { email: "", password: "" } });`
    4. Convert the existing onSubmit to: `const onSubmit = form.handleSubmit(async (values) => { ... existing signIn logic using values.email and values.password ... })`
    5. Wrap the <form> element with <Form {...form}>
    6. Replace the email <Input> with a <FormField> that renders the input and <FormMessage> below it.
    7. Repeat for password input.
    8. The existing loading state (submitting / setSubmitting) stays as-is — it controls the button disabled state (already correct per Plan 04 verification).

    Signup.tsx:
    1. Same import additions.
    2. At module scope: define signupSchema and SignupFormValues.
       signupSchema fields (read Signup.tsx to confirm actual field names):
       - fullName: z.string().min(2, "Name must be at least 2 characters")
       - email: z.string().email("Enter a valid email")
       - password: z.string().min(6, "Password must be at least 6 characters")
       - role: z.enum(["doctor", "patient"])
       - specialty: z.string().optional()
    3. Replace individual useState for each field with useForm.
    4. Convert onSubmit to form.handleSubmit.
    5. Wrap form JSX with <Form {...form}>, add <FormMessage> under each input.
    6. Preserve the existing role selector and specialty conditional field logic.

    CRITICAL: Read each file fully before editing to understand the current form structure. Do not remove the loading/submitting state — only replace the controlled input state (email/password/etc. individual useStates) with react-hook-form.
  </action>
  <verify>
    <automated>npm test -- src/test/form-validation.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>form-validation.test.tsx Login and Signup tests pass GREEN; both forms show inline error messages for invalid input; valid input still submits correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Zod validation to PracticeSetup, Onboarding, Settings invite, and PatientDetail cost</name>
  <files>
    src/pages/public/PracticeSetup.tsx
    src/pages/public/Onboarding.tsx
    src/pages/doctor/Settings.tsx
    src/pages/doctor/PatientDetail.tsx
  </files>
  <behavior>
    - PracticeSetup: practiceName shorter than 2 chars → validation error shown inline; no DB write
    - PracticeSetup: invalid address (too short) → validation error shown inline
    - Onboarding: required fields empty → appropriate validation errors shown inline
    - Settings invite: non-email string → "Enter a valid email" shown inline; invite not sent
    - PatientDetail cost: negative number entered → validation error; cost not saved
    - All schemas at module scope
  </behavior>
  <action>
    PracticeSetup.tsx:
    - Read the file to understand the multi-step structure before editing.
    - Define practiceSetupSchema at module scope with fields matching the actual form inputs.
    - Replace per-field useState with useForm + zodResolver.
    - Each step that has form fields gets <FormField> + <FormMessage> wrappers.
    - The multi-step `step` state stays as-is (it is navigation state, not form state).

    Onboarding.tsx:
    - Read the file to identify all form fields.
    - Define onboardingSchema at module scope.
    - Convert to useForm + zodResolver.
    - Add FormMessage under required fields.

    Settings.tsx (invite email only):
    - This form likely already has some useState for inviteEmail.
    - Add a small inviteSchema: `const inviteSchema = z.object({ inviteEmail: z.string().email("Enter a valid email") });`
    - Convert the invite input to react-hook-form.
    - FormMessage shows the email error inline.
    - The rest of Settings.tsx (specialty, logo upload, etc.) does not need react-hook-form — those are individual handlers, not forms. Leave them with their existing state.

    PatientDetail.tsx (estimated cost input):
    - Add `const costSchema = z.object({ estimatedCost: z.number().min(0, "Cost cannot be negative") });`
    - Convert the cost input to useForm + zodResolver.
    - Add FormMessage below the input.

    For all files: place all schema definitions at the top of the file, before the component function definition.
  </action>
  <verify>
    <automated>npm test -- src/test/form-validation.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 files have Zod schemas at module scope; all targeted form fields display inline validation errors; form-validation.test.tsx passes GREEN; npm test full suite passes</done>
</task>

</tasks>

<verification>
npm test -- src/test/form-validation.test.tsx
Expected: all GREEN.

npx tsc --noEmit
Expected: no TypeScript errors from Zod type inference changes.
</verification>

<success_criteria>
- form-validation.test.tsx passes GREEN
- Login, Signup, PracticeSetup, Onboarding, Settings invite, PatientDetail cost all use zodResolver
- All schemas defined at module scope (zero schemas inside component render)
- Invalid data shows inline error messages via FormMessage
- Valid submissions still work correctly (no regressions in auth/signup flow)
- Full npm test suite passes
</success_criteria>

<output>
After completion, create .planning/phases/01-platform-professionalization/01-07-SUMMARY.md
</output>
